java集合框架分为两种
一种是单列集合Collection，一个元素(数据)
一种是双列集合map.两个元素(键值对)

还没理解的
lambda
comparable接口
TreeSet自定义类型排序

Collection<E>分为Lise<E>和set<E>



List<E>(特点 有序的  可重复的 有索引的  )
分为  ArrayList<E>   和   linkedList<E>

 ArrayList<E>底层原理
基于数组实现
查询速度快（根据索引查）
删除效率低
添加效率极低

为什么每次扩容都是1.5倍
1.内存空间的复用
5倍扩容，在连续扩容数次后（通常是第4次或第5次），之前释放的旧内存空间总和就会大于当前所需的扩容空间。
这样，JVM 就可以直接复用并合并之前被回收的旧内存碎片，极大地提高了内存利用率，符合高德纳几何级数分配的最佳实践。
2.执行效率上看，1.5 倍可以通过高效的‘位运算’来实现
扩容的核心代码是 int newCapacity = oldCapacity + (oldCapacity >> 1);。这里的 >> 1 是二进制右移一位，等同于除以 2。
Java 团队并没有使用浮点数乘法（如 * 1.5），因为浮点数计算极度消耗 CPU 时钟周期。
而通过位运算加法，只需要 1 个时钟周期就能完成，将扩容本身的计算耗时降到了最低。
3.‘空间’与‘时间’的平衡
扩容倍数过大（比如 3 倍、4 倍），虽然扩容次数变少了、省了拷贝时间，但会造成极大的空间浪费（很多格子空着吃干饭）。
如果扩容倍数过小（比如 1.1 倍），虽然空间利用率极高，但在追加大量数据时会引发频繁的数组全量拷贝（System.arraycopy），
导致毁灭性的性能延迟。

追问 1：那为什么 Vector 默认是 2 倍扩容？
标准反击：Vector 是 Java 1.0 年代的古老产物，当时还没有考虑到这么完美的几何级数内存复用理论。
它的设计相对粗暴，而且可以通过构造函数手动指定扩容步长（capacityIncrement）。
ArrayList 是 Java 1.2 引入的，吸取了早期的教训，才优化成了 1.5 倍。

追问 2：如果在实际开发中，我知道我有 10000 条数据要一次性装入 ArrayList，该怎么做？
标准反击：绝对不能让它从默认的 10 开始进行 1.5 倍的自动扩容，因为这会触发十几次全量拷贝，严重拉低性能。
我们应该在初始化时直接指定容量：List<Integer> list = new ArrayList<>(10000);。
这样可以直接一步到位，规避掉所有中间的扩容开销。

linkedList<E>底层原理
基于双链表实现
查询慢 增删相对较快 首尾元素进行增删改查速度极快
应用场景 
用来设计队列 先进先出，后进后出 比如奶茶排队
设计栈，后进先出，先进后出      比如弹夹内的子弹
linkedList<E>添加了很多首尾操作的特有方法
addfirst（）   push方法
removeFirst （）  pop方法

链表分为单向链表和双向链表

链表中的节点是独立的对象，内存当中不连续，每个节点包含数据值和下一个节点的地址
单向列表  特点  查询慢 从头开始找              增删相对快
双向列表  查询慢 增删相对较快 首尾元素进行增删改查速度极快



Arrays.toString(k) 的主要作用是：将数组的内容转换为一个易读的字符串格式。
Arrays.toString() 拼接出来的字符串有非常固定的格式：
开头带一个左中括号 [
元素之间用逗号和空格 ,  分隔
结尾带一个右中括号 ]
Arrays.toString() 只适用于一维数组
直接打印数组会出现地址值
静态方法
Arrays.toString() 是数组生命周期的终点站


toArray()是 集合（Collection） 特有的方法 。它的核心作用是：把一个“集合”变成一个“数组” 。
只有集合（List, Set等）可以调用
实例方法

这三个方法都是 Java 集合框架（特别是 List 体系）中用来往容器里添加数据的核心方法
add(E e)（添加单个元素）   Collection
addAll(Collection<? extends E> c) （把另一个集合拆开追加到末尾）   Collection
addAll(int index, Collection<? extends E> c) (在指定位置批量插队）  List

遍历一个集合（特别是像 Set 这种没有索引、不能用传统 for(int i=0;... ) 循环的集合），你就必须请出“迭代器（Iterator）”。
迭代器  Collection
Iterator 
hasNext() 判断队伍里后面还有没有乘客了。如果后面还有人，返回 true；如果后面空空如也（全检完了），返回 false。
next（）  作用：做两件事。第一，把当前这个乘客（元素）抓取并返回给你；第二，检票员往后移动一格，对准下一个乘客。






Set<E>(特点 无序的  不重复的 无索引的  )
分为 HashSet<E>  和  TreeSet<E>
 TreeSet<E> 特点(按照大小默认升序排序 不重复的 无索引的  )
HashSet<E>  (特点 无序的  不重复的 无索引的  )
又可以分为   LinkedHashSet<E>
  LinkedHashSet<E>特点是 有序的 不重复的 无索引的  
 

HashSet<E>
基于哈希表实现
哈希表是一种增删改查数据，性能都较好的数据结构
jdk8之前
哈希表=数组+链表
jdk8之后
哈希表=数组+链表+红黑树
当链表长度超过8且数组长度>=64时自动将链表转成红黑树      进一步提高操作数据的性能

创建一个默认长度16的数组
默认加载因子0.75
使用元素的哈希值对数组长度求余计算出存入的位置
判断当前位置是否为null
为null直接存入，
不为null调用equals方法比较
相等不存
不相等存入数组 新元素挂在老元素下面

哈希值是一个int类型的数值，java中每个对象都有哈希值。
java 所有对象都可以调用Object类的hashCode方法返回该对象的哈希值
特点  同一个对象调用hashCode（） 返回的哈希值是一样的
不同的对象哈希值一般不同，但也有可能会相同（哈希碰撞）


如果希望对象内容是重复的就重写方法hashCode（）和equals()方法

二叉树
二叉树任意节点的度<=2

度是每个节点的子节点数
树高 树的总层数
根节点  最顶层的节点
左子节点
右子节点
左子树
右子树

二叉查找树（二叉排序树）
小的存左边 ，大的存右边， 一样的不存
如果数据已经是排列好的了，会导致查询的性能跟单链表一样，查询速度慢

平衡二叉树
满足查找二叉树的规则，让树尽可能矮小，提高查询数据的性能

红黑树
自动平衡二叉树


LinkedHashSet<E>
基于哈希表实现
但是每个元素额外的多了一个双链表的机制记录它前后的元素的位置


TreeSet<E>
基于红黑树实现的排序
对于数值类型 按照数值本身大小进行升序排序
对于字符串类型 默认按照首字符的编号升序排序
对于自定义类型 默认是无法直接排序的
调用接口Comparable
TreeSet自带比较器，默认使用集合自带的比较器排序



集合的并发修改问题
使用迭代器遍历集合，同时又删除集合中的数据，程序会出现并发修改异常的错误。
要么for循环遍历删除之后i--，要么倒着遍历删除，要么用迭代器自带的方法

